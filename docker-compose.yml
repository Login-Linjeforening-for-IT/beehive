services:
  beehive:
    container_name: beehive
    build:
      context: ./
    ports:
      - "3000:3000"
    volumes:
      - "./:/app"
      - "/app/node_modules"
    environment:
      - "APP_ENV=development"
