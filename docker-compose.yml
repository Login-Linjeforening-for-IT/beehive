services:
  beehive:
    container_name: beehive
    image: beehive
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - workerbee_default
      - tekkom_bot_default
      - beekeeper_default
    mem_limit: 2g
    cpus: "2.0"

networks:
  workerbee_default:
    external: true
  tekkom_bot_default:
    external: true
  beekeeper_default:
    external: true